# 技术栈升级说明（2026-04 全量升级）

> 工程名：`pandora-arch`（原 `ddd-platform`）· 父包名：`cn.pandora`
> 统一版本：`1.0.0`
> 基准日期：2026-04-17

---

## 一、升级总览

| 组件 | 旧版本 | 新版本 | 变化 |
|------|--------|--------|------|
| JDK | 17 | **25** | LTS，启用 ZGC 分代 + 虚拟线程默认 |
| Spring Framework | 6.1 | **7.0** | 最低 JDK 17，支持虚拟线程全链路 |
| Spring Boot | 3.2.3 | **4.0.5** | 全新可观测性抽象、Bean Property AOT |
| Spring Cloud | 2023.0.0 | **2025.0.0** | 与 SB 4.0 匹配 |
| Spring Cloud Alibaba | 2023.0.1 | **2025.1.0.0** | Nacos 3 支持 |
| ORM | MyBatis-Plus 3.5.5 | **MyBatis-Flex 1.11.6** | 轻量级、无反射开销 |
| MySQL 驱动 | 8.x | **9.6.0** | MySQL 9 协议、caching_sha2 默认 |
| Redis | 7.0 / Lettuce | **7.4.2 + Redisson 3.50** | 统一连接，内置分布式锁/限流/布隆 |
| MongoDB | — | **5.0.x（新增）** | 文档数据存储 |
| Kafka | — | **3.8.1（新增）** | KRaft 无 ZK、异步消息解耦 |
| Lombok | 1.18.32 | **1.18.40** | JDK 25 兼容（修复 ExceptionInInitializerError） |
| Lettuce | 6.3.x | **6.5.5.RELEASE** | 实际锁定版本（显式写 pom，避免 BOM 指向未发布版本） |
| MinIO Java SDK | 8.5.7 | **8.5.17** | 实际锁定到 Maven Central 现存最新版 |
| MapStruct | 1.5.5 | **1.6.3** | records + sealed 支持 |
| Hutool | 5.8.25 | **5.8.35** | — |
| MinIO Java SDK | 8.5.7 | **8.5.18** | — |
| OAuth2 AS | 1.2.2 | **1.4.0** | 匹配 SB4 |
| JJWT | 0.12.5 | **0.12.6** | 安全修复 |
| SpringDoc | 2.3.0 | **2.7.0** | OpenAPI 3.1 |

---

## 二、工程命名规范

### 2.1 GroupId / Package / Module

| 维度 | 规则 |
|------|------|
| `groupId` | `cn.pandora` |
| 根 `artifactId` | `pandora-arch` |
| 子模块 `artifactId` | `pandora-<功能>`（`pandora-common`、`pandora-auth`、`pandora-start` 等） |
| Java 包 | `cn.pandora.<模块>.<子层>`（如 `cn.pandora.infrastructure.security`） |
| 模块版本 | 统一 `1.0.0`，由根 `pom.xml` `dependencyManagement` 管控 |

### 2.2 模块清单（10 个）

```
pandora-arch (父)
├── pandora-common         # 通用：Result / 异常 / 工具 / 基础注解
├── pandora-common-cloud   # 微服务通用：Feign / Sentinel / Nacos 共用
├── pandora-domain         # DDD 领域层：聚合根 / 值对象 / 领域事件
├── pandora-application    # DDD 应用层：用例编排 / DTO
├── pandora-infrastructure # DDD 基础设施：持久化 / 缓存 / 锁 / MQ / 安全
├── pandora-api            # DDD 接口层：Controller / Swagger
├── pandora-auth           # 独立认证服务（OAuth2 AS）
├── pandora-start          # 单体启动模块
├── pandora-gateway        # 微服务网关（Spring Cloud Gateway）
└── pandora-service        # 微服务聚合父模块
    └── pandora-service-user
```

---

## 三、关键代码变更

### 3.1 MyBatis-Plus → MyBatis-Flex

| 旧 API | 新 API |
|--------|--------|
| `@TableName("t")` | `@Table("t")` |
| `@TableId(type = IdType.AUTO)` | `@Id(keyType = KeyType.Auto)` |
| `@TableField("col")` | `@Column("col")` |
| `@TableField(fill=FieldFill.INSERT)` | 删除；改为 `InsertListener` 自动注入 |
| `@TableLogic` | `@Column(isLogicDelete = true)` |
| `com.baomidou.mybatisplus.core.mapper.BaseMapper` | `com.mybatisflex.core.BaseMapper` |
| `ServiceImpl<M, E>` | `com.mybatisflex.spring.service.impl.ServiceImpl` |
| `baseMapper.xxx()` | `mapper.xxx()`（protected 字段改名） |
| `LambdaQueryWrapper.eq(E::f, v)` | `QueryWrapper.create().eq("f", v)` |
| `MybatisPlusInterceptor + InnerInterceptor` | 标准 MyBatis `Interceptor` |
| `MetaObjectHandler` | `InsertListener` / `UpdateListener`（见 `MybatisFlexConfig`） |

> Tip：MyBatis-Flex 的 `mybatis-flex-processor` 会为每个 DO 生成 `XxxTableDef`，支持类型安全的列引用；当前代码暂未启用此生成器以减少耦合，可按需打开。

### 3.2 新增组件

| 组件 | 位置 | 作用 |
|------|------|------|
| `RedissonConfig` | `cn.pandora.infrastructure.config` | Redisson 客户端与 Spring Data Redis 共享连接 |
| `DistributedLockService` | `cn.pandora.infrastructure.lock` | 分布式锁封装（互斥、读写锁、延时队列） |
| `KafkaConfig` | `cn.pandora.infrastructure.config` | 幂等生产者 + 手动 ack 消费者 |
| `KafkaProducerService` | `cn.pandora.infrastructure.mq` | 异步 / 同步发送封装 |
| `MongoConfig` | `cn.pandora.infrastructure.config` | Mongo 审计开关 |
| `MybatisFlexConfig` | `cn.pandora.infrastructure.config` | Flex 全局配置 + Listener + 数据权限拦截器注册 |

### 3.3 JDK 25 关键特性启用

- **ZGC Generational**：`-XX:+UseZGC -XX:+ZGenerational`，毫秒级 STW
- **Virtual Threads**：Spring Boot 4 默认在 `spring.threads.virtual.enabled=true` 时启用
- **Pattern Matching for switch / record patterns**：代码中大量使用 `switch (x) { case A a -> ...; }`
- **Sealed Classes**：`DataScopeStrategy` 等策略类可密封以强约束

---

## 四、部署拓扑（新）

```
                        ┌────────────────┐
                        │  API Gateway   │
                        │ pandora-gateway│ (9999)
                        └───────┬────────┘
                                │ Nacos 3 注册发现
          ┌─────────────────────┼─────────────────────┐
          ▼                     ▼                     ▼
  ┌──────────────┐      ┌──────────────┐      ┌────────────────┐
  │  pandora-    │      │  pandora-    │      │  pandora-      │
  │  auth (9100) │      │  start (8080)│      │  service-user  │
  └──────┬───────┘      └──────┬───────┘      └──────┬─────────┘
         └──────────────────────┼───────────────────┘
                                │
      ┌────────┬────────┬───────┴───────┬────────┬────────┐
      ▼        ▼        ▼               ▼        ▼        ▼
  MySQL 9   Redis 7    Kafka 3.8    MongoDB 5  MinIO    Sentinel
  主从      Cluster    KRaft        副本集     对象存储 流控
```

---

## 五、配置示例（完整）

见：
- `pandora-start/src/main/resources/application.yml`（单体）
- `pandora-auth/src/main/resources/application.yml`（认证）
- `pandora-service/pandora-service-user/src/main/resources/application.yml`（用户微服务）
- `pandora-gateway/src/main/resources/application.yml`（网关）

---

## 六、升级后验证矩阵

| 验证项 | 命令 / 方式 | 预期 |
|--------|------------|------|
| 根 pom 版本 | `mvn help:evaluate -Dexpression=project.version` | `1.0.0` |
| JDK | `java -version` | `25` |
| MyBatis-Flex 生效 | 查 `sys_user` 写入，`deleted=0` 自动 | 新记录 deleted 默认 0 |
| 分布式锁 | 并发调 `lockService.runWithLock` | 串行执行 |
| Kafka 收发 | `POST /api/demo/kafka/send` → 查监听日志 | 日志出现 offset/partition |
| Redis Cluster | `redis-cli -c -p 7001 CLUSTER INFO` | `cluster_state:ok` |
| MySQL 主从 | 主库写 → 从库 `SELECT` | 秒级同步 |

---

## 七、相关文档

| 文档 | 路径 |
|------|------|
| 总架构 | `docs/架构设计方案.md` |
| 模块详解 | `docs/模块组件详解.md` |
| 启动顺序 | `docs/服务启动顺序与配置指南.md` |
| 部署指南 | `docs/服务部署指南.md` |
| MySQL 主从 | `docs/运维/MySQL主从部署.md` |
| Redis 集群 | `docs/运维/Redis集群部署.md` |
| Kafka on Win11 | `docs/运维/Kafka部署与使用-Windows11.md` |
| CI / CD 流水线 | `docs/运维/CICD流水线部署.md` |

---

## 八、升级遗留风险与建议

1. **JDK 25 + Lombok**：必须使用 `lombok 1.18.40+`，否则注解处理器编译失败。
2. **Spring Boot 4.x**：`@EntityScan` 等部分类包路径变更；`javax` 已全面退出。业务里已删除 `JpaConfig.java`，JPA 自动扫描由 `@SpringBootApplication` 的默认包扫描承担。
3. **Sentinel 适配器包名变化**：Spring MVC 6/7 下 `BlockExceptionHandler` 位置改动，因此 `SentinelConfig` 改为 `@RestControllerAdvice` 统一捕获 `BlockException` 及子类，更稳定。
4. **MyBatis-Flex QueryWrapper**：当前用字符串列名，后续可打开 `mybatis-flex-processor` 使用 `TableDef` 获得类型安全。
5. **Redisson 作为 Redis 连接工厂**：若以前业务直接注入 `LettuceConnectionFactory`，需改为 `RedisConnectionFactory`。
6. **Kafka 未默认启用**：`pandora.kafka.enabled=false`，按需开启以避免本地启动时连不到 Broker。
7. **MongoDB 未默认启用**：`pandora.mongodb.enabled=false`，同上。
8. **生产环境**：Redisson 集群模式必须覆盖 `application.yml` 中的 singleServerConfig，否则会误连单机。
9. **依赖版本现实性**：某些 Spring Boot 4.0.5 BOM 指向的传递依赖（如 `lettuce-core 6.7.2.RELEASE`、`minio 8.5.18`）在 Maven Central 尚未发布，已在根 `pom.xml` 显式锁定到可用版本（见上表）。
